/**
 * Callback to be passed to promise constructors
 *
 * @typedef {function} PromiseExecutor
 *
 * @param {?function(value:*)} resolve - Function to resolve the promise
 * @param {?function(reason:Error)} reject - Function to reject the promise
 * @returns {*} Ignored
 */
